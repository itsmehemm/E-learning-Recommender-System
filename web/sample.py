topic = ["database", "index", "machine", "learning"]
token = ["machine", "learning", "cloud"]

for i in range(0, len(topic)):
	for j in range(0, len(token)):
		if(topic[i] == token[j]):
			print("success")
